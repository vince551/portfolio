document.addEventListener('DOMContentLoaded',()=>{
if(window.AOS)AOS.init({once:true,duration:850,offset:90});
const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];
const menuBtn=$('#menu-btn'),closeBtn=$('#close-menu-btn'),menu=$('#mobile-menu'),mobileLinks=$$('.mobile-link'),navbar=$('#navbar');
function setMenu(open){if(!menu)return;menu.classList.toggle('translate-x-full',!open);menu.setAttribute('aria-hidden',String(!open));menuBtn?.setAttribute('aria-expanded',String(open));document.body.style.overflow=open?'hidden':'';if(open)closeBtn?.focus();else menuBtn?.focus()}
menuBtn?.addEventListener('click',()=>setMenu(true));closeBtn?.addEventListener('click',()=>setMenu(false));mobileLinks.forEach(x=>x.addEventListener('click',()=>setMenu(false)));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&menu&&!menu.classList.contains('translate-x-full'))setMenu(false)});
const progress=$('#scroll-progress');function scrollUI(){const max=document.documentElement.scrollHeight-innerHeight;progress&&(progress.style.width=(max>0?scrollY/max*100:0)+'%');navbar?.classList.toggle('shadow-2xl',scrollY>45);navbar?.classList.toggle('bg-dark-900/95',scrollY>45)}addEventListener('scroll',scrollUI,{passive:true});scrollUI();
const sections=$$('section[id]'),links=$$('.nav-link');const sectionObserver=new IntersectionObserver(entries=>entries.forEach(e=>{if(!e.isIntersecting)return;links.forEach(l=>{const active=l.getAttribute('href')==='#'+e.target.id;l.classList.toggle('text-white',active);l.classList.toggle('text-gray-400',!active);l.setAttribute('aria-current',active?'page':'false')})}),{rootMargin:'-35% 0px -55% 0px'});sections.forEach(s=>sectionObserver.observe(s));
$$('[data-tilt]').forEach(card=>{card.addEventListener('pointermove',e=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(900px) rotateX(${(-y*5).toFixed(2)}deg) rotateY(${(x*5).toFixed(2)}deg) translateY(-4px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});
const form=$('#contact-form'),status=$('#form-status'),submit=$('#submit-btn');form?.addEventListener('submit',e=>{e.preventDefault();const name=form.elements.name.value.trim(),email=form.elements.email.value.trim(),message=form.elements.message.value.trim();if(!name||!email||!message){status.textContent='Please complete all fields.';status.className='text-sm text-brand-pink';return}if(!/^\S+@\S+\.\S+$/.test(email)){status.textContent='Please enter a valid email address.';status.className='text-sm text-brand-pink';return}submit.disabled=true;submit.innerHTML='Opening email...';const subject=encodeURIComponent('Portfolio enquiry from '+name),body=encodeURIComponent(`Name: ${name}\nEmail: ${email}\n\n${message}`);location.href=`mailto:architectvince7@gmail.com?subject=${subject}&body=${body}`;status.textContent='Your email app should open with the message ready to send.';status.className='text-sm text-brand-cyan';setTimeout(()=>{submit.disabled=false;submit.innerHTML='Send Enquiry <i class="ph-bold ph-paper-plane-right"></i>'},1600)});
const year=$('#year');if(year)year.textContent=new Date().getFullYear();
function init3D(){if(!window.THREE||matchMedia('(prefers-reduced-motion: reduce)').matches)return;const host=$('#three-bg');if(!host)return;const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(55,innerWidth/innerHeight,.1,100);camera.position.z=8;const renderer=new THREE.WebGLRenderer({alpha:true,antialias:true,powerPreference:'low-power'});renderer.setPixelRatio(Math.min(devicePixelRatio,1.5));renderer.setSize(innerWidth,innerHeight);host.appendChild(renderer.domElement);const group=new THREE.Group();scene.add(group);const geo=new THREE.IcosahedronGeometry(2.6,1),mat=new THREE.MeshBasicMaterial({color:0x8b5cf6,wireframe:true,transparent:true,opacity:.09});group.add(new THREE.Mesh(geo,mat));const dots=new THREE.BufferGeometry(),count=140,pos=new Float32Array(count*3);for(let i=0;i<count;i++){pos[i*3]=(Math.random()-.5)*18;pos[i*3+1]=(Math.random()-.5)*12;pos[i*3+2]=(Math.random()-.5)*10}dots.setAttribute('position',new THREE.BufferAttribute(pos,3));scene.add(new THREE.Points(dots,new THREE.PointsMaterial({color:0x06b6d4,size:.025,transparent:true,opacity:.45})));let mx=0,my=0;addEventListener('pointermove',e=>{mx=(e.clientX/innerWidth-.5)*.6;my=(e.clientY/innerHeight-.5)*.35},{passive:true});function frame(t){group.rotation.x+=.00025;group.rotation.y+=.00045;group.rotation.y+=(mx-group.rotation.y)*.002;group.rotation.x+=(my-group.rotation.x)*.002;renderer.render(scene,camera);requestAnimationFrame(frame)}requestAnimationFrame(frame);addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)},{passive:true})}init3D();
const projectData=[{title:'EFAC — Education For All Children',meta:'EDUCATION / DIGITAL PLATFORM',desc:'A technology initiative centered on academic support, mentorship, career pathways, leadership, wellbeing and technology-enabled education.',image:'images/efac-case-study.svg',repo:'https://github.com/vince551/EFAC',label:'View EFAC repository',accent:'cyan',details:['Education & mentorship','Youth empowerment','Digital resources']},{title:'KCA Digital Platform',meta:'COMMUNITY / WEB SOLUTION',desc:'A structured digital experience focused on clearer information access, reporting workflows, community resources and responsible interface design.',image:'images/kca-case-study.svg',repo:'https://github.com/vince551/kca',label:'View KCA repository',accent:'purple',details:['Information architecture','Reporting workflow','Responsive web']},{title:'Pitch Kings — DLS Tournament Manager',meta:'SPORT / MANAGEMENT PLATFORM',desc:'A tournament-management platform for DLS communities covering teams, players, fixtures, results, notifications and match-day activity.',image:'images/pitch-kings-case-study.svg',repo:'https://github.com/vince551/Pitch-kings',label:'View Pitch Kings repository',accent:'pink',details:['Firebase backend','Fixtures & standings','Players & teams']}];
function mountProjectCaseStudies(){const cards=$$('.project-card');if(cards.length<3)return;cards.slice(0,3).forEach((card,i)=>{const p=projectData[i];card.classList.add('case-study-card');card.innerHTML=`<div class="project-number">0${i+1}</div><button class="case-image-button" type="button" aria-label="Open ${p.title} case study"><img class="case-image" src="${p.image}" alt="${p.title} project interface mockup" loading="lazy"></button><div class="p-7"><div class="project-meta">${p.meta}</div><h3>${p.title}</h3><p>${p.desc}</p><div class="chips">${p.details.map(x=>`<span>${x}</span>`).join('')}</div><div class="case-actions"><button class="case-open" type="button" data-project="${i}">View case study <i class="ph-bold ph-arrow-up-right"></i></button><a href="${p.repo}" target="_blank" rel="noopener noreferrer" class="case-repo">GitHub <i class="ph-bold ph-github-logo"></i></a></div></div>`;card.addEventListener('pointermove',e=>{if(matchMedia('(prefers-reduced-motion: reduce)').matches)return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width-.5,y=(e.clientY-r.top)/r.height-.5;card.style.transform=`perspective(1000px) rotateX(${(-y*4).toFixed(2)}deg) rotateY(${(x*4).toFixed(2)}deg) translateY(-5px)`});card.addEventListener('pointerleave',()=>card.style.transform='')});$$('.case-open,.case-image-button').forEach(btn=>btn.addEventListener('click',()=>openCaseStudy(Number(btn.dataset.project??btn.closest('.project-card')?.querySelector('.case-open')?.dataset.project))));}
function openCaseStudy(index){const p=projectData[index];if(!p)return;let modal=$('#case-modal');if(!modal){modal=document.createElement('div');modal.id='case-modal';modal.className='case-modal';modal.innerHTML='<div class="case-dialog" role="dialog" aria-modal="true" aria-label="Project case study"><button class="case-close" type="button" aria-label="Close case study"><i class="ph ph-x"></i></button><div id="case-content"></div></div>';document.body.appendChild(modal);modal.querySelector('.case-close').addEventListener('click',closeCaseStudy);modal.addEventListener('click',e=>{if(e.target===modal)closeCaseStudy()})}$('#case-content').innerHTML=`<div class="case-modal-grid"><div><img src="${p.image}" class="case-modal-image" alt="${p.title} interface mockup"></div><div><div class="section-kicker">CASE STUDY / ${String(index+1).padStart(2,'0')}</div><h2>${p.title}</h2><p class="case-modal-desc">${p.desc}</p><div class="case-modal-list">${p.details.map((x,n)=>`<div><span>0${n+1}</span>${x}</div>`).join('')}</div><a class="premium-btn case-modal-link" href="${p.repo}" target="_blank" rel="noopener noreferrer">Explore source <i class="ph-bold ph-arrow-up-right"></i></a></div></div>`;modal.classList.add('is-open');document.body.style.overflow='hidden';modal.querySelector('.case-close').focus()}
function closeCaseStudy(){const modal=$('#case-modal');if(modal){modal.classList.remove('is-open');document.body.style.overflow=''}}
function mountSocialRail(){if($('#social-rail'))return;const rail=document.createElement('aside');rail.id='social-rail';rail.className='social-rail';rail.setAttribute('aria-label','Social links');rail.innerHTML=`<a href="https://github.com/vince551" target="_blank" rel="noopener noreferrer" class="social-rail-link github" aria-label="GitHub" data-label="GitHub"><i class="ph-fill ph-github-logo"></i></a><a href="https://www.instagram.com/vincey.dev/" target="_blank" rel="noopener noreferrer" class="social-rail-link instagram" aria-label="Instagram" data-label="Instagram"><i class="ph-fill ph-instagram-logo"></i></a><a href="https://wa.me/254718896349" target="_blank" rel="noopener noreferrer" class="social-rail-link whatsapp" aria-label="WhatsApp" data-label="WhatsApp"><i class="ph-fill ph-whatsapp-logo"></i></a><a href="https://x.com/vincey_dev" target="_blank" rel="noopener noreferrer" class="social-rail-link x" aria-label="X" data-label="X"><span>𝕏</span></a>`;document.body.appendChild(rail)}
mountProjectCaseStudies();mountSocialRail();
});